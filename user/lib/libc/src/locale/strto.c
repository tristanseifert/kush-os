#include <string.h>
#include <stdlib.h>
#include <locale.h>

/*
 * Implementations of the various locale-specific string conversion routines. Currently, these
 * just punt on through to the non-locale aware implementations.
 *
 * TODO: implement correctly
 */
